# CI-only fixture — not real credentials. Fills in just enough of values.schema.json's
# required fields so `ct lint` / `helm template` can render this chart in CI.
settings:
  omc:
    actor:
      id: 33333333-3333-4333-8333-333333333333
    auth:
      jwt:
        audience: ci-audience
        secret: "ci-test-secret-ci-test-secret-ci-test-secret-ci-test-secret-ci-t"
        issuer: ci-issuer
        userId: ci-user-id
        userName: ci-user-name
  notify:
    api:
      key: "ci_key-11111111-1111-4111-8111-111111111111-22222222-2222-4222-8222-222222222222"
  zgw:
    auth:
      jwt:
        secret: ci-zgw-secret
        issuer: ci-zgw-issuer
        userId: ci-zgw-user-id
        userName: ci-zgw-user-name
      key:
        openklant: ci-openklant-key
    endpoint:
      openNotificaties: https://open-notificaties.example.com
      openZaak: https://open-zaak.example.com
      openKlant: https://open-klant.example.com
